import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#E91E63",
    },
    secondary: {
      main: "#242424",
    },
    background: {
        default: "#171717",
    }
  },
});

export default theme;
